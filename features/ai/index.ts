/**
 * AI Feature Module
 *
 * This module owns:
 * - RAG knowledge retrieval
 * - LLM response generation
 * - Compliance explanations
 * - Document content generation
 *
 * Rules:
 * - All responses must originate from retrieved knowledge — never model memory
 * - AI must never invent requirements, costs, deadlines, or agencies
 * - Uncertainty must be surfaced, not hidden
 */
